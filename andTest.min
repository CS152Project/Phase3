function main;
beginparams
endparams
beginlocals
endlocals
beginbody
  if x >= y and a <= b then
   j := i + i; 
endif;
   if a <= c and d >= f then
     j := i+i;
endif;
   if n == m and j == k then 
     x := 5 - 2;
endif;
endbody 
